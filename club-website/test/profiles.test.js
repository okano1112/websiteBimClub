const test = require('node:test'); const assert = require('node:assert/strict'); const fs = require('node:fs'); const path = require('node:path');
const root = path.join(__dirname, '..');
test('Phase 5 profile migration and allowlisted API exist', () => { const sql = fs.readFileSync(path.join(root, 'database/controlled-p05-profiles.sql'), 'utf8'); const route = fs.readFileSync(path.join(root, 'src/routes/profiles.js'), 'utf8'); assert.match(sql, /member_profiles/); assert.match(route, /SELECT id, username, full_name, avatar_url, role/); assert.match(route, /router\.put\('\/me'/); assert.match(route, /เฉพาะผู้ดูแลระบบเท่านั้น/); assert.doesNotMatch(route, /recovery_phone/); });
test('Profile page uses namespaced selectors and escapes rendered content', () => { const html = fs.readFileSync(path.join(root, 'public/page/profile.html'), 'utf8'); const js = fs.readFileSync(path.join(root, 'public/js/profile.js'), 'utf8'); assert.match(html, /member-profile-page/); assert.match(js, /const esc/); assert.match(js, /api\/profiles/); });
